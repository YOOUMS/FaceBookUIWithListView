import 'package:flutter/material.dart';

import 'facebookUI.dart';

void main() {
  runApp(MaterialApp(
    home: facebookUI(),
    debugShowCheckedModeBanner: false,
  ));

  // runApp(MaterialApp(home: facebookUI2()));
}
